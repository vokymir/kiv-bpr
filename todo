libs:
- GUI
  - Dear ImGui + ImPlot?
- Graph
  - igraph

a) generate graph
b) select mode (toppling rules, graph generation method etc) & parameters (size)
c) run 1 simulation (visualization, stepping, ...)
d) run batch simulations (collect stats)
e) display stats (load, process)
